name: verify

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Create repo-local virtual environment
        run: uv venv --python ${{ matrix.python-version }}

      - name: Install scraper dependencies
        run: uv pip install --python .venv requests beautifulsoup4

      - name: Run skill structure checks
        run: sh tests/structure/test_skills.sh

      - name: Run template structure checks
        run: sh tests/structure/test_templates.sh

      - name: Run unit tests
        run: .venv/bin/python -m unittest discover -s tests -p 'test_*.py' -v
